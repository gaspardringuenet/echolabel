# Testing

## Dataloader

Test datasets:

* `/data/echopype_ds_MVBS.zarr` - small (~1000 ESDU) subset of the output of the echopype data from the krill example notebook.
* `/data/imos_nc_dir/` - contains very small (2 x 100 ESDU) contiguous subset of the ABRAÇOS II survey.
